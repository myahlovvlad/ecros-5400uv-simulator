# Р СѓРєРѕРІРѕРґСЃС‚РІРѕ РїРѕ Р·Р°РїСѓСЃРєСѓ

## РўСЂРµР±РѕРІР°РЅРёСЏ

- Node.js `18+`
- npm
- РґРѕСЃС‚СѓРї Рє GitHub, РµСЃР»Рё РЅСѓР¶РµРЅ `push`

## РЈСЃС‚Р°РЅРѕРІРєР° Р·Р°РІРёСЃРёРјРѕСЃС‚РµР№

```bash
npm install
```

## Р›РѕРєР°Р»СЊРЅС‹Р№ Р·Р°РїСѓСЃРє

```bash
npm run dev
```

РђРґСЂРµСЃ РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ:

```text
http://127.0.0.1:3000
```

## РЎР±РѕСЂРєР°

```bash
npm run build
```

## РўРµСЃС‚С‹

```bash
npm test -- --run
```

## Windows

Р”Р»СЏ Р»РѕРєР°Р»СЊРЅРѕРіРѕ СЃС‚Р°СЂС‚Р° С‚Р°РєР¶Рµ РјРѕР¶РЅРѕ РёСЃРїРѕР»СЊР·РѕРІР°С‚СЊ:

```bat
scripts\\init.bat
```

РЎРєСЂРёРїС‚ Р·Р°РїСѓСЃРєР°РµС‚ `npm.cmd` Рё РЅРµ РёСЃРїРѕР»СЊР·СѓРµС‚ РїСЂРѕР±Р»РµРјРЅС‹Рµ batch-РєРѕРЅСЃС‚СЂСѓРєС†РёРё СЃ Р±РёС‚РѕР№ РєРѕРґРёСЂРѕРІРєРѕР№.

## Р§Р°СЃС‚С‹Рµ РїСЂРѕР±Р»РµРјС‹

### `ERR_CONNECTION_REFUSED`

РџСЂРѕРІРµСЂСЊС‚Рµ, С‡С‚Рѕ dev-СЃРµСЂРІРµСЂ РґРµР№СЃС‚РІРёС‚РµР»СЊРЅРѕ РїРѕРґРЅСЏС‚:

```bash
npm run dev
```

Р’ РїСЂРѕРµРєС‚Рµ Р·Р°С„РёРєСЃРёСЂРѕРІР°РЅС‹:

- `host: 127.0.0.1`
- `port: 3000`
- `strictPort: true`

### РЎС‚СЂР°РЅРёС†Р° РІС‹РіР»СЏРґРёС‚ РєР°Рє СЃС‹СЂРѕР№ HTML

РџСЂРѕРІРµСЂСЊС‚Рµ, С‡С‚Рѕ Р·Р°РіСЂСѓР¶Р°РµС‚СЃСЏ `src/styles.css` С‡РµСЂРµР· `src/main.jsx`.

### РћС€РёР±РєРё TypeScript РїРѕ `config/tsconfig.node.json`

Р’ РєРѕРЅС„РёРіРµ РґРѕР»Р¶РµРЅ Р±С‹С‚СЊ РІРєР»СЋС‡С‘РЅ `noEmit`, С‡С‚РѕР±С‹ TypeScript РЅРµ РїС‹С‚Р°Р»СЃСЏ РїРµСЂРµР·Р°РїРёСЃР°С‚СЊ РІС…РѕРґРЅС‹Рµ РєРѕРЅС„РёРіРё.

## Р РµРєРѕРјРµРЅРґСѓРµРјС‹Р№ СЂР°Р±РѕС‡РёР№ С†РёРєР»

1. `npm install`
2. `npm run dev`
3. `npm test -- --run`
4. `npm run build`
5. `git status`


